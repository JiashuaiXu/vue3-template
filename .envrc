# Direnv configuration for Nix development environment
# Requires nix-direnv integration to work properly

# Watch flake files to reload when changed
watch_file flake.nix
watch_file flake.lock

# Use nix integration if available
if command -v nix-direnv >/dev/null 2>&1; then
  use nix
else
  # Fallback: let user know to use nix develop directly
  export DIRENV_NIX_MISSING=1
fi